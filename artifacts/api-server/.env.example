# API server environment
# Copy to .env and replace placeholders with real values.

PORT=9090
DATABASE_URL=postgres://username:password@localhost:5432/finance_navigator
JWT_SECRET=replace-with-a-long-random-secret
